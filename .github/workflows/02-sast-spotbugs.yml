name: 02 - SAST (SpotBugs + FindSecBugs)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  spotbugs-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven

    - name: Run SpotBugs with FindSecBugs
      run: cd via-backend && mvn clean verify spotbugs:spotbugs
      continue-on-error: true

    - name: Generate HTML Report
      if: always()
      run: |
        cd via-backend
        if [ -f target/spotbugsXml.xml ]; then
          echo "✓ SpotBugs XML report found"
          ls -lh target/spotbugsXml.xml
        else
          echo "⚠ No SpotBugs XML report generated"
        fi

    - name: Upload SpotBugs Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: spotbugs-report
        path: via-backend/target/spotbugsXml.xml
        retention-days: 30

    - name: Summary - SAST Scan Results
      if: always()
      run: |
        echo "## 🔍 SAST Scan Results (SpotBugs + FindSecBugs)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Configuration" >> $GITHUB_STEP_SUMMARY
        echo "- **Scanner**: SpotBugs 4.8.5.0 + FindSecBugs 1.13.0" >> $GITHUB_STEP_SUMMARY
        echo "- **Effort Level**: max" >> $GITHUB_STEP_SUMMARY
        echo "- **Threshold**: medium" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Report" >> $GITHUB_STEP_SUMMARY
        if [ -f via-backend/target/spotbugsXml.xml ]; then
          ISSUES=$(grep -c "<BugInstance" via-backend/target/spotbugsXml.xml 2>/dev/null || echo "0")
          echo "- **Total Issues Found**: $ISSUES" >> $GITHUB_STEP_SUMMARY
          echo "- **Report Path**: \`via-backend/target/spotbugsXml.xml\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Download**: Check Artifacts tab" >> $GITHUB_STEP_SUMMARY
        else
          echo "- **Status**: Report not generated" >> $GITHUB_STEP_SUMMARY
        fi

